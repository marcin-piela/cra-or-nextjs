import React from 'react'

export default ({ children }) => (
  <div
    style={{
      textAlign: 'right',
      width: '70%',
      marginTop: '50px',
      fontSize: '1.15rem'
    }}>
    {children}
  </div>
);